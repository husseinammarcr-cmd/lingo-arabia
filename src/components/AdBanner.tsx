// AdBanner disabled - third-party ad networks (highperformanceformat, effectivegatecpm)
// caused Google to flag site as "Compromised Site"
// Only Google AdSense should be used once the site is reviewed and approved

interface AdBannerProps {
  className?: string;
}

const AdBanner = ({ className }: AdBannerProps) => {
  return null;
};

export default AdBanner;
